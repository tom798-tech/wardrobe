🔴 严重漏洞（高危，必须修复）
1. 鉴权形同虚设 — 几乎所有接口都不需要登录
问题位置：SaTokenConfig.java 第 17 行
java
运行
.excludePathPatterns("/register", "/login", "/clothes/**", "/cart/**", "/order/**", 
    "/user/**", "/vector/**", "/ai/**", "/review/**" ...);
风险：购物车、订单、用户信息、评论、AI 接口…… 核心业务接口全部放行，不需要登录就能调用。
任何人都能查看所有订单、所有用户信息
任何人都能往任意用户的购物车加商品、清空购物车
任何人都能调用 AI 接口消耗资源
修复建议：只放行公开接口（商品列表、登录、注册），其余全部需要鉴权。
2. 密码明文存储
问题位置：UserService.java + wardrobe.sql
代码中密码直接明文比对：
java
运行
if (!currentUser.getPassword().equals(userVo.getPassword())) { ... }
数据库里 admin/admin123、zhangsan/123123 全是明文。
风险：一旦数据库被拖库，所有用户密码直接泄露，且用户大概率在其他网站也用同一密码。
修复建议：使用 BCrypt 加密存储，登录时用 BCryptPasswordEncoder.matches() 比对。
3. 水平越权 — 能查看 / 操作任意用户的数据
问题位置：OrderController、CartController、UserController
典型例子：
java
运行
// 只要传 userId 就能查任何人的订单
@GetMapping("/user/{userId}")
public List<Order> getOrderByUserId(@PathVariable Integer userId) {
    return orderService.findByUserId(userId);
}
风险：
把 userId 从 1 改成 2，就能看别人的订单、地址、手机号
购物车同理，可以给别人加商品、清空别人购物车
用户信息接口可以遍历所有用户数据
修复建议：从 StpUtil.getLoginId() 获取当前登录用户 ID，而不是从前端传参。
4. 垂直越权 — 普通用户也能调用管理员接口
问题位置：UserController、OrderController、ClothesController
java
运行
// 管理端增删改商品的接口，没有校验角色
@PostMapping("/clothes")
public String addClothes(@RequestBody Clothes clothes) { ... }

@DeleteMapping("/user/{id}")
public String deleteUser(@PathVariable Integer id) { ... }
风险：普通用户登录后，直接调用 /clothes POST 就能上架商品，调用 /user/{id} DELETE 就能删用户。
修复建议：用 Sa-Token 的角色权限注解 @SaCheckRole("admin") 保护管理接口。
5. 登录接口返回完整用户信息（含密码）
问题位置：LoginController.java 第 35 行
java
运行
return loginUser;  // 返回整个 User 对象，包含 password 字段
风险：登录成功后，前端能拿到用户的明文密码，F12 就能看到。
修复建议：返回 DTO 对象，只返回必要字段（id、用户名、token），剔除密码。
🟠 高危漏洞
6. 无接口限流 — 可暴力破解密码、批量注册
问题：/login 和 /register 没有任何频率限制。
风险：
暴力破解：用脚本跑密码字典，无限试错
批量注册：一秒注册几百个账号，污染数据库
爬虫：商品列表、用户列表接口可被无限抓取
修复建议：
用 Redis + 滑动窗口算法做限流（登录接口：单 IP 5 次 / 分钟）
或者 Nginx 层配置 limit_req_zone
7. 无图形验证码
问题：登录、注册都没有验证码。
风险：自动化脚本可以无阻碍地批量注册、爆破密码、刷评论。
修复建议：
注册、登录失败 3 次后弹出图形验证码
评论、下单等关键操作加行为验证
8. 文件上传漏洞 — 可上传恶意脚本
问题位置：UploadController.java
java
运行
// 只判断了文件不为空，没校验文件类型
if (file == null || file.isEmpty()) { return "文件为空"; }
风险：
可以上传 .jsp、.php 等 webshell 脚本
如果上传目录可被 Web 直接访问，服务器会被直接拿下
修复建议：
白名单校验后缀（只允许 jpg/png/gif/webp）
校验文件头魔数（防止改后缀绕过）
上传目录禁止执行权限，用独立域名 / OSS 存储
🟡 中危漏洞
9. XSS 跨站脚本风险
问题：评论、商品描述、用户名等用户输入内容，后端没有做转义直接存库，前端直接渲染。
风险：
攻击者在评论里写 <script>偷cookie</script>
其他用户看到评论时，脚本自动执行，账号被盗
修复建议：
后端统一加 XSS 过滤器，转义特殊字符
前端用 v-text 代替 v-html
富文本内容用白名单标签过滤
10. 默认弱密码
问题：
MySQL：root/123456
Redis：123456
管理员：admin/admin123
测试用户：zhangsan/123123
风险：弱密码是被暴力破解的重灾区，尤其是暴露在公网时。
修复建议：生产环境强制修改复杂密码，长度 12 位以上，包含大小写数字符号。
11. 无 CSRF 防护
问题：Sa-Token 默认没有开启 CSRF 防护。
风险：攻击者诱导登录用户访问恶意网站，可以在用户不知情下以用户身份发请求（比如改密码、下单）。
修复建议：开启 Sa-Token 的 CSRF Token 校验，或者校验 Referer 头。
12. SQL 注入（潜在风险）
现状：MyBatis-Plus 默认用 #{} 预编译，大部分接口是安全的。
风险点：
如果未来有人写原生 SQL 用了 ${} 拼接，就会有注入漏洞
排序字段如果直接从前端传入且不做白名单校验，也可能注入
修复建议：
代码审查禁止使用 ${} 拼接用户输入
动态排序字段做白名单校验
开启 MyBatis-Plus 的 .checkSqlInjection()
🟢 低危 / 配置问题
13. 数据库使用 root 账号
建议：创建专用业务账号，只授予必要的 SELECT/INSERT/UPDATE/DELETE 权限。
14. 错误信息直接暴露
问题：e.printStackTrace() 直接打印堆栈，异常信息可能返回给前端。
建议：统一异常处理，只返回友好提示，不暴露技术细节。
15. 缺少 HTTPS
问题：本地开发是 HTTP，上线如果也是 HTTP，密码和 Token 会被明文传输。
建议：生产环境必须配置 HTTPS（TLS 1.2+）。
16. Token 无过期策略
建议：配置 Sa-Token 的 Token 有效期，支持自动续期或强制重新登录。
📋 修复优先级建议
表格
优先级	漏洞	修复工作量
P0 紧急	鉴权放行过多、水平越权、密码明文、敏感信息泄露	1-2 天
P1 高	接口限流、验证码、文件上传校验、垂直越权	2-3 天
P2 中	XSS 防护、CSRF、弱密码	1-2 天
P3 低	数据库权限、异常处理、HTTPS	配置即可